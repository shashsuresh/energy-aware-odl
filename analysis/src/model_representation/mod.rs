/// An enum corresponding to the different bias-update ratios we support
pub mod channel_ratio;
/// A neural network layer
pub mod layer;
/// Wrapper on Layer used to represent for different types of layers
pub mod layer_descriptor;
/// A neural network
pub mod model;
/// Parameters of neural network layers and methods to extract information from them
pub mod params;
