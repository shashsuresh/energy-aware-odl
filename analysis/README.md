# Analysis

## Description

This is the analysis program that can analyse a model and provide the user with details on the memory and computation cost of a provided update scheme. Furthermore, it includes a scheme-searcher component, which can be used to generate the best update scheme for a given model with user defined constraints and optimization parameters.

## Usage
